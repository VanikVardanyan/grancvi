# Epic 8.2 — Docker Production Deployment + CI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Автоматизировать деплой tg-bot на Hetzner VPS через GitHub Actions с правильными failure modes и observability.

**Architecture:** CI (ruff+mypy+pytest) на PR и push в main; при зелёном CI deploy-workflow идёт SSH под `deploy`-юзером в `/opt/tg-bot`, делает `git reset --hard origin/main` + `docker compose -f base -f prod up -d --build`. Миграции применяются entrypoint'ом контейнера; Docker HEALTHCHECK пингает Telegram `getMe`; Sentry ловит необработанные исключения.

**Tech Stack:** Docker Compose (base + prod override), GitHub Actions (`workflow_run` chaining), `appleboy/ssh-action@v1`, bash idempotent bootstrap, sentry-sdk, stdlib urllib для healthcheck.

**Spec:** `docs/superpowers/specs/2026-04-22-epic-8-2-deploy-design.md`

---

## Pre-flight: verify starting state

- [ ] **Step 0.1: Activate venv и проверить зелёные гейты**

```bash
source .venv/bin/activate
ruff check . && ruff format --check . && mypy src/ && pytest -q
```
Expected: `All checks passed`, mypy `Success`, `311 passed` (или больше — зависит от добавленных за время тестов).

- [ ] **Step 0.2: Убедиться, что локальный main впереди origin на ≥41 коммит**

```bash
git log origin/main..HEAD --oneline | wc -l
```
Expected: число ≥41.

- [ ] **Step 0.3: Убедиться, что рабочее дерево чистое**

```bash
git status
```
Expected: `working tree clean` (кроме файлов, которые сейчас редактируются IDE — `.env` и т.п., но они в `.gitignore`).

---

## Task 1: Add sentry-sdk dependency + conditional init in main.py

**Files:**
- Modify: `pyproject.toml`
- Modify: `src/main.py`
- Create: `tests/test_main_sentry.py`

**Rationale:** Sentry — первое, о чём жалеет solo-dev без алертов. Config-поле `settings.sentry_dsn` уже есть, интеграция — 5 строк. TDD: тест проверяет, что `sentry_sdk.init` вызывается только когда DSN задан.

- [ ] **Step 1.1: Добавить sentry-sdk в deps**

Отредактировать `pyproject.toml`, в блок `[project] dependencies`:

```toml
dependencies = [
    "aiogram>=3.4,<4",
    "sqlalchemy[asyncio]>=2.0,<3",
    "asyncpg>=0.29",
    "alembic>=1.13",
    "redis>=5.0",
    "apscheduler>=3.10,<4",
    "pydantic>=2.6",
    "pydantic-settings>=2.2",
    "structlog>=24.1",
    "aiogram-i18n>=1.4",
    "fluent-compiler>=1.1",
    "sentry-sdk>=2.0",
]
```

- [ ] **Step 1.2: Обновить lock-файл**

```bash
uv sync
```
Expected: `Resolved ... packages`, sentry-sdk добавлен.

- [ ] **Step 1.3: Написать тесты для Sentry conditional init**

Создать `tests/test_main_sentry.py`:

```python
from __future__ import annotations

from unittest.mock import MagicMock, patch

import pytest


@patch("sentry_sdk.init")
def test_sentry_not_called_when_dsn_is_none(mock_init: MagicMock) -> None:
    from src.main import _init_sentry_if_configured

    _init_sentry_if_configured(dsn=None)

    mock_init.assert_not_called()


@patch("sentry_sdk.init")
def test_sentry_not_called_when_dsn_is_empty(mock_init: MagicMock) -> None:
    from src.main import _init_sentry_if_configured

    _init_sentry_if_configured(dsn="")

    mock_init.assert_not_called()


@patch("sentry_sdk.init")
def test_sentry_called_with_dsn(mock_init: MagicMock) -> None:
    from src.main import _init_sentry_if_configured

    _init_sentry_if_configured(dsn="https://public@sentry.io/1234")

    mock_init.assert_called_once()
    kwargs = mock_init.call_args.kwargs
    assert kwargs["dsn"] == "https://public@sentry.io/1234"
    assert kwargs["traces_sample_rate"] == 0.0
```

- [ ] **Step 1.4: Запустить тесты, убедиться что падают**

```bash
pytest tests/test_main_sentry.py -v
```
Expected: `ImportError: cannot import name '_init_sentry_if_configured'` (или аналогично).

- [ ] **Step 1.5: Реализовать `_init_sentry_if_configured` в src/main.py**

Отредактировать `src/main.py`, добавить функцию и вызвать её из `main()`. Финальный вид:

```python
from __future__ import annotations

import asyncio
import logging
from functools import partial

import structlog
from aiogram import Bot, Dispatcher
from apscheduler.triggers.cron import CronTrigger

from src.config import settings
from src.db.base import SessionMaker
from src.fsm_storage import build_fsm_storage
from src.handlers import build_root_router
from src.middlewares.db import DbSessionMiddleware
from src.middlewares.lang import LangMiddleware
from src.middlewares.user import UserMiddleware
from src.scheduler.jobs import expire_pending_appointments, send_due_reminders
from src.scheduler.setup import build_scheduler


def configure_logging() -> None:
    logging.basicConfig(level=settings.log_level, format="%(message)s")
    structlog.configure(
        processors=[
            structlog.contextvars.merge_contextvars,
            structlog.processors.add_log_level,
            structlog.processors.TimeStamper(fmt="iso"),
            structlog.processors.JSONRenderer(),
        ],
    )


def _init_sentry_if_configured(dsn: str | None) -> None:
    if not dsn:
        return
    import sentry_sdk

    sentry_sdk.init(dsn=dsn, traces_sample_rate=0.0)


log: structlog.stdlib.BoundLogger = structlog.get_logger()


def build_dispatcher() -> Dispatcher:
    storage = build_fsm_storage()
    dp = Dispatcher(storage=storage)
    dp.update.middleware(DbSessionMiddleware(SessionMaker))
    dp.update.middleware(UserMiddleware())
    dp.update.middleware(LangMiddleware())
    dp.include_router(build_root_router())
    return dp


async def main() -> None:
    configure_logging()
    _init_sentry_if_configured(settings.sentry_dsn)
    bot = Bot(token=settings.bot_token)
    dp = build_dispatcher()

    scheduler = build_scheduler()
    scheduler.add_job(
        partial(send_due_reminders, bot=bot, session_factory=SessionMaker),
        trigger=CronTrigger(minute="*"),
        id="send_due_reminders",
        replace_existing=True,
    )
    scheduler.add_job(
        partial(expire_pending_appointments, bot=bot, session_factory=SessionMaker),
        trigger=CronTrigger(minute="*/5"),
        id="expire_pending_appointments",
        replace_existing=True,
    )

    log.info("bot_starting")
    scheduler.start()
    try:
        await dp.start_polling(bot)
    finally:
        scheduler.shutdown(wait=True)
        await bot.session.close()


if __name__ == "__main__":
    asyncio.run(main())
```

- [ ] **Step 1.6: Запустить тесты, убедиться что проходят**

```bash
pytest tests/test_main_sentry.py -v
```
Expected: 3 passed.

- [ ] **Step 1.7: Запустить все гейты**

```bash
ruff check . && ruff format --check . && mypy src/ && pytest -q
```
Expected: всё зелёное.

- [ ] **Step 1.8: Commit**

```bash
git add pyproject.toml uv.lock src/main.py tests/test_main_sentry.py
git commit -m "feat(obs): wire Sentry SDK with conditional init on DSN"
```

---

## Task 2: scripts/healthcheck.py + tests

**Files:**
- Create: `scripts/healthcheck.py`
- Create: `tests/test_healthcheck.py`

**Rationale:** Docker HEALTHCHECK вызывает этот скрипт раз в минуту. Использует stdlib `urllib` — без новых зависимостей. Три branch'а логики: нет токена / сетевая ошибка / Telegram `ok=false`. TDD — мок `urllib.request.urlopen`.

- [ ] **Step 2.1: Создать директорию scripts/ (если нет)**

```bash
mkdir -p scripts
```

- [ ] **Step 2.2: Написать failing-тесты**

Создать `tests/test_healthcheck.py`:

```python
from __future__ import annotations

import json
import urllib.error
from io import BytesIO
from unittest.mock import MagicMock, patch

import pytest


def _fake_response(payload: dict) -> MagicMock:
    resp = MagicMock()
    resp.__enter__ = MagicMock(return_value=resp)
    resp.__exit__ = MagicMock(return_value=False)
    resp.read = MagicMock(return_value=json.dumps(payload).encode())
    return resp


def test_healthcheck_no_token(monkeypatch, capsys) -> None:
    monkeypatch.delenv("BOT_TOKEN", raising=False)

    from scripts.healthcheck import main

    rc = main()

    assert rc == 1
    assert "BOT_TOKEN not set" in capsys.readouterr().err


def test_healthcheck_empty_token(monkeypatch) -> None:
    monkeypatch.setenv("BOT_TOKEN", "")

    from scripts.healthcheck import main

    assert main() == 1


@patch("urllib.request.urlopen")
def test_healthcheck_success(mock_open: MagicMock, monkeypatch) -> None:
    monkeypatch.setenv("BOT_TOKEN", "dummy")
    mock_open.return_value = _fake_response({"ok": True, "result": {"id": 1}})

    from scripts.healthcheck import main

    assert main() == 0


@patch("urllib.request.urlopen")
def test_healthcheck_telegram_ok_false(mock_open: MagicMock, monkeypatch) -> None:
    monkeypatch.setenv("BOT_TOKEN", "dummy")
    mock_open.return_value = _fake_response({"ok": False, "description": "bad"})

    from scripts.healthcheck import main

    assert main() == 1


@patch("urllib.request.urlopen")
def test_healthcheck_network_error(mock_open: MagicMock, monkeypatch, capsys) -> None:
    monkeypatch.setenv("BOT_TOKEN", "dummy")
    mock_open.side_effect = urllib.error.URLError("unreachable")

    from scripts.healthcheck import main

    assert main() == 1
    assert "getMe failed" in capsys.readouterr().err


@patch("urllib.request.urlopen")
def test_healthcheck_timeout(mock_open: MagicMock, monkeypatch, capsys) -> None:
    monkeypatch.setenv("BOT_TOKEN", "dummy")
    mock_open.side_effect = TimeoutError("too slow")

    from scripts.healthcheck import main

    assert main() == 1
    assert "timed out" in capsys.readouterr().err


@patch("urllib.request.urlopen")
def test_healthcheck_non_json(mock_open: MagicMock, monkeypatch) -> None:
    monkeypatch.setenv("BOT_TOKEN", "dummy")
    resp = MagicMock()
    resp.__enter__ = MagicMock(return_value=resp)
    resp.__exit__ = MagicMock(return_value=False)
    resp.read = MagicMock(return_value=b"<html>error</html>")
    mock_open.return_value = resp

    from scripts.healthcheck import main

    assert main() == 1
```

- [ ] **Step 2.3: Запустить тесты — убедиться, что падают (модуль не существует)**

```bash
pytest tests/test_healthcheck.py -v
```
Expected: `ModuleNotFoundError: No module named 'scripts.healthcheck'`.

- [ ] **Step 2.4: Создать `scripts/__init__.py` (пустой — чтобы импорт в тестах работал)**

```bash
touch scripts/__init__.py
```

- [ ] **Step 2.5: Реализовать `scripts/healthcheck.py`**

```python
"""Exits 0 if bot can reach Telegram API, 1 otherwise.

Called by Docker HEALTHCHECK every 60s.
Uses stdlib only to avoid adding an HTTP-client dependency.
"""
from __future__ import annotations

import json
import os
import sys
import urllib.error
import urllib.request


def main() -> int:
    token = os.environ.get("BOT_TOKEN")
    if not token:
        print("BOT_TOKEN not set", file=sys.stderr)
        return 1
    url = f"https://api.telegram.org/bot{token}/getMe"
    try:
        with urllib.request.urlopen(url, timeout=8.0) as resp:
            body = resp.read()
    except urllib.error.URLError as exc:
        print(f"getMe failed: {exc}", file=sys.stderr)
        return 1
    except TimeoutError as exc:
        print(f"getMe timed out: {exc}", file=sys.stderr)
        return 1

    try:
        data = json.loads(body)
    except json.JSONDecodeError as exc:
        print(f"getMe response not JSON: {exc}", file=sys.stderr)
        return 1

    if not data.get("ok"):
        print(f"Telegram returned ok=false: {data}", file=sys.stderr)
        return 1
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2.6: Запустить тесты, убедиться что проходят**

```bash
pytest tests/test_healthcheck.py -v
```
Expected: 7 passed.

- [ ] **Step 2.7: Проверить гейты**

```bash
ruff check . && ruff format --check . && mypy src/ && pytest -q
```
Expected: всё зелёное. (Обрати внимание: mypy запускается только на `src/`, но если на `scripts/` нужно — `mypy src/ scripts/`.)

- [ ] **Step 2.8: Добавить scripts/ в mypy проверку**

Отредактировать `pyproject.toml`, найти секцию `[tool.mypy]`:

```toml
[tool.mypy]
python_version = "3.12"
strict = true
plugins = ["pydantic.mypy"]
exclude = ["migrations/", "build/", "dist/"]
```

Оставить как есть — mypy через аргумент `src/` проверяет только src/. Для scripts/ отдельным вызовом — не обязательно, `ruff` покрывает базовые проблемы.

- [ ] **Step 2.9: Commit**

```bash
git add scripts/ tests/test_healthcheck.py
git commit -m "feat(deploy): add healthcheck.py for Docker HEALTHCHECK (urllib-based)"
```

---

## Task 3: .env.prod.example + update .env.example

**Files:**
- Create: `.env.prod.example`
- Modify: `.env.example`

**Rationale:** Текущий `.env.example` имеет 7 ключей, но `docker-compose.yml` хардкодит `POSTGRES_USER/PASSWORD/DB=botik`. В проде хардкодить пароль нельзя. Прод-шаблон отдельный, чтоб не сломать локальный dev.

- [ ] **Step 3.1: Создать `.env.prod.example`**

```bash
# Bot
BOT_TOKEN=

# Admin (comma-separated Telegram user ids who can register as master)
ADMIN_TG_IDS=

# Observability
SENTRY_DSN=
LOG_LEVEL=INFO

# Database (generate password with: openssl rand -base64 24)
POSTGRES_USER=botik
POSTGRES_PASSWORD=
POSTGRES_DB=botik
DATABASE_URL=postgresql+asyncpg://botik:REPLACE_WITH_POSTGRES_PASSWORD@postgres:5432/botik

# Redis
REDIS_URL=redis://redis:6379/0

# Locale
DEFAULT_TIMEZONE=Asia/Yerevan
```

- [ ] **Step 3.2: Обновить `.env.example` для dev-паритета**

Отредактировать, финальный вид:

```bash
BOT_TOKEN=
DATABASE_URL=postgresql+asyncpg://botik:botik@postgres:5432/botik
REDIS_URL=redis://redis:6379/0
ADMIN_TG_IDS=
LOG_LEVEL=INFO
SENTRY_DSN=
DEFAULT_TIMEZONE=Asia/Yerevan

# Only used by docker-compose.prod.yml — keep as "botik" for dev parity
POSTGRES_USER=botik
POSTGRES_PASSWORD=botik
POSTGRES_DB=botik
```

- [ ] **Step 3.3: Проверить, что `.env.example` и `.env.prod.example` в `.gitignore` НЕ попадают (должны коммититься)**

```bash
git check-ignore -v .env.example .env.prod.example 2>&1 | head -5
```
Expected: обе строки вернут пустой вывод (файлы НЕ игнорируются). Если какая-то игнорируется — исправь `.gitignore`.

- [ ] **Step 3.4: Commit**

```bash
git add .env.example .env.prod.example
git commit -m "feat(deploy): add .env.prod.example and align dev .env.example"
```

---

## Task 4: docker-compose.prod.yml override

**Files:**
- Create: `docker-compose.prod.yml`

**Rationale:** Прод-override убирает expose портов pg/redis (internal only), ставит `restart: always`, лимит логов, Docker HEALTHCHECK на app. Базовый `docker-compose.yml` остаётся для локальной разработки без изменений.

- [ ] **Step 4.1: Создать `docker-compose.prod.yml`**

```yaml
services:
  postgres:
    ports: []
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  redis:
    ports: []
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  app:
    restart: always
    healthcheck:
      test: ["CMD", "python", "/app/scripts/healthcheck.py"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 30s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
```

- [ ] **Step 4.2: Валидировать merge через `docker compose config`**

```bash
docker compose -f docker-compose.yml -f docker-compose.prod.yml config
```
Expected: YAML merge проходит без ошибок, видно `restart: always` у всех трёх сервисов, pg/redis `ports: []`.

- [ ] **Step 4.3: Убедиться, что локальный dev не сломался**

```bash
docker compose config
```
Expected: без prod-override, pg/redis экспонируют порты, у app нет healthcheck.

- [ ] **Step 4.4: Commit**

```bash
git add docker-compose.prod.yml
git commit -m "feat(deploy): add docker-compose.prod.yml override"
```

---

## Task 5: Update Dockerfile (entrypoint + healthcheck + scripts copy)

**Files:**
- Modify: `Dockerfile`

**Rationale:** Entrypoint автоматически применяет миграции перед стартом бота (`alembic upgrade head && exec python -m src.main`). HEALTHCHECK в Dockerfile — для standalone `docker run` (compose-override дублирует).

- [ ] **Step 5.1: Отредактировать `Dockerfile`**

Финальный вид:

```dockerfile
# syntax=docker/dockerfile:1.7

FROM python:3.12-slim AS base

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    UV_LINK_MODE=copy \
    UV_COMPILE_BYTECODE=1

COPY --from=ghcr.io/astral-sh/uv:0.5.4 /uv /usr/local/bin/uv

WORKDIR /app

COPY pyproject.toml uv.lock ./
RUN uv sync --frozen --no-dev --no-install-project

COPY src ./src
COPY migrations ./migrations
COPY scripts ./scripts
COPY alembic.ini ./

ENV PATH="/app/.venv/bin:$PATH"

HEALTHCHECK --interval=60s --timeout=10s --start-period=30s --retries=3 \
    CMD python /app/scripts/healthcheck.py || exit 1

CMD ["sh", "-c", "alembic upgrade head && exec python -m src.main"]
```

- [ ] **Step 5.2: Локальная sanity-сборка (x86 Mac — build завершится, запускать контейнер не нужно)**

```bash
docker build -t tg-bot-test .
```
Expected: build успешен, `Successfully tagged tg-bot-test`. Если ARM Mac — добавь `--platform=linux/amd64` для x86-совместимости или `--platform=linux/arm64` для прод-паритета.

- [ ] **Step 5.3: Проверить HEALTHCHECK в образе**

```bash
docker inspect tg-bot-test --format='{{.Config.Healthcheck.Test}}'
```
Expected: `[CMD-SHELL python /app/scripts/healthcheck.py || exit 1]`.

- [ ] **Step 5.4: Проверить CMD в образе**

```bash
docker inspect tg-bot-test --format='{{.Config.Cmd}}'
```
Expected: `[sh -c alembic upgrade head && exec python -m src.main]`.

- [ ] **Step 5.5: Удалить test-образ**

```bash
docker rmi tg-bot-test
```

- [ ] **Step 5.6: Commit**

```bash
git add Dockerfile
git commit -m "feat(deploy): Dockerfile entrypoint with alembic migrate + HEALTHCHECK"
```

---

## Task 6: scripts/bootstrap-vps.sh

**Files:**
- Create: `scripts/bootstrap-vps.sh`

**Rationale:** Идемпотентный shell-скрипт для первичной настройки VPS. Запускается под root сразу после создания VM. Ставит Docker, создаёт юзера `deploy`, hardens sshd, открывает порт 22 в UFW, клонирует репу.

- [ ] **Step 6.1: Создать `scripts/bootstrap-vps.sh`**

```bash
#!/usr/bin/env bash
# Идемпотентный bootstrap для свежей Ubuntu 24.04 VM.
# Запускать под root на VPS один раз.
set -euo pipefail

REPO_URL="https://github.com/VanikVardanyan/grancvi.git"
APP_DIR="/opt/tg-bot"
DEPLOY_USER="deploy"

echo "==> Step 1/6: apt update + essentials"
apt-get update
apt-get upgrade -y
apt-get install -y ca-certificates curl gnupg git ufw

echo "==> Step 2/6: Docker Engine + compose plugin"
if ! command -v docker >/dev/null 2>&1; then
    install -m 0755 -d /etc/apt/keyrings
    curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
    chmod a+r /etc/apt/keyrings/docker.asc
    echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] \
https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo "${VERSION_CODENAME}") stable" \
        > /etc/apt/sources.list.d/docker.list
    apt-get update
    apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
else
    echo "  docker already installed — skipping"
fi

echo "==> Step 3/6: deploy user + docker group + SSH keys"
if ! id -u "${DEPLOY_USER}" >/dev/null 2>&1; then
    useradd -m -s /bin/bash "${DEPLOY_USER}"
fi
usermod -aG docker "${DEPLOY_USER}"
mkdir -p "/home/${DEPLOY_USER}/.ssh"
if [ -f /root/.ssh/authorized_keys ]; then
    cp /root/.ssh/authorized_keys "/home/${DEPLOY_USER}/.ssh/authorized_keys"
fi
chown -R "${DEPLOY_USER}:${DEPLOY_USER}" "/home/${DEPLOY_USER}/.ssh"
chmod 700 "/home/${DEPLOY_USER}/.ssh"
chmod 600 "/home/${DEPLOY_USER}/.ssh/authorized_keys" || true

echo "==> Step 4/6: UFW — only SSH"
ufw allow 22/tcp
ufw default deny incoming
ufw default allow outgoing
ufw --force enable

echo "==> Step 5/6: sshd hardening"
sed -i 's/^#\?PasswordAuthentication.*/PasswordAuthentication no/' /etc/ssh/sshd_config
sed -i 's/^#\?PermitRootLogin.*/PermitRootLogin prohibit-password/' /etc/ssh/sshd_config
systemctl restart ssh

echo "==> Step 6/6: clone repo under ${DEPLOY_USER}"
mkdir -p "${APP_DIR}"
chown "${DEPLOY_USER}:${DEPLOY_USER}" "${APP_DIR}"
if [ ! -d "${APP_DIR}/.git" ]; then
    sudo -u "${DEPLOY_USER}" git clone "${REPO_URL}" "${APP_DIR}"
else
    echo "  repo already cloned — skipping"
fi

echo ""
echo "==============================================="
echo " Bootstrap complete."
echo ""
echo " Next steps:"
echo "   1. Create /opt/tg-bot/.env from template:"
echo "      sudo -u ${DEPLOY_USER} cp ${APP_DIR}/.env.prod.example ${APP_DIR}/.env"
echo ""
echo "   2. Edit /opt/tg-bot/.env, fill BOT_TOKEN, SENTRY_DSN, etc."
echo "      Generate POSTGRES_PASSWORD:"
echo "        openssl rand -base64 24"
echo "      Then update both POSTGRES_PASSWORD and DATABASE_URL in .env."
echo ""
echo "   3. First build & start:"
echo "      cd ${APP_DIR}"
echo "      sudo -u ${DEPLOY_USER} docker compose \\"
echo "        -f docker-compose.yml -f docker-compose.prod.yml up -d --build"
echo ""
echo "   4. Generate deploy-only SSH key locally on your Mac:"
echo "      ssh-keygen -t ed25519 -f ~/.ssh/grancvi-deploy -N '' -C 'grancvi-deploy'"
echo "   5. Append the pub key to /home/${DEPLOY_USER}/.ssh/authorized_keys on this VPS."
echo "   6. Add to GitHub Secrets (repo Settings → Secrets and variables → Actions):"
echo "        SSH_HOST        = this VPS's IPv4"
echo "        SSH_USER        = ${DEPLOY_USER}"
echo "        SSH_PRIVATE_KEY = contents of ~/.ssh/grancvi-deploy (private key)"
echo "==============================================="
```

- [ ] **Step 6.2: Сделать скрипт исполняемым**

```bash
chmod +x scripts/bootstrap-vps.sh
```

- [ ] **Step 6.3: Syntax-check скрипта**

```bash
bash -n scripts/bootstrap-vps.sh
```
Expected: ноль вывода, exit 0.

- [ ] **Step 6.4: Shellcheck (если установлен)**

```bash
command -v shellcheck >/dev/null && shellcheck scripts/bootstrap-vps.sh || echo "shellcheck not installed — skipping"
```
Expected: либо нет установлен, либо проходит без warnings.

- [ ] **Step 6.5: Commit**

```bash
git add scripts/bootstrap-vps.sh
git commit -m "feat(deploy): add idempotent bootstrap-vps.sh"
```

---

## Task 7: GitHub Actions — CI workflow

**Files:**
- Create: `.github/workflows/ci.yml`

**Rationale:** PR и push в main триггерят ruff + format check + mypy + pytest на ephemeral Postgres/Redis. Защищает main от сломанного кода.

- [ ] **Step 7.1: Создать директорию**

```bash
mkdir -p .github/workflows
```

- [ ] **Step 7.2: Создать `.github/workflows/ci.yml`**

```yaml
name: ci

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gates:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: botik
          POSTGRES_PASSWORD: botik
          POSTGRES_DB: botik_test
        ports: ["5432:5432"]
        options: >-
          --health-cmd="pg_isready -U botik"
          --health-interval=3s
          --health-timeout=3s
          --health-retries=20
      redis:
        image: redis:7-alpine
        ports: ["6379:6379"]
        options: >-
          --health-cmd="redis-cli ping"
          --health-interval=3s
          --health-timeout=3s
          --health-retries=20
    env:
      DATABASE_URL: postgresql+asyncpg://botik:botik@localhost:5432/botik_test
      REDIS_URL: redis://localhost:6379/0
      BOT_TOKEN: dummy
      ADMIN_TG_IDS: "0"
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: "0.5.4"

      - name: Set up Python 3.12
        run: uv python install 3.12

      - name: Sync deps
        run: uv sync --frozen

      - name: Ruff lint
        run: uv run ruff check .

      - name: Ruff format check
        run: uv run ruff format --check .

      - name: Mypy
        run: uv run mypy src/

      - name: Pytest
        run: uv run pytest -q
```

- [ ] **Step 7.3: YAML syntax-check**

```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"
```
Expected: ноль вывода.

- [ ] **Step 7.4: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: add ruff+mypy+pytest workflow on PR and push to main"
```

---

## Task 8: GitHub Actions — Deploy workflow

**Files:**
- Create: `.github/workflows/deploy.yml`

**Rationale:** После зелёного CI на main — SSH на VPS, `git reset --hard origin/main`, `docker compose up -d --build`. `workflow_run` гарантирует, что деплой не идёт при красном CI.

- [ ] **Step 8.1: Создать `.github/workflows/deploy.yml`**

```yaml
name: deploy

on:
  workflow_run:
    workflows: [ci]
    branches: [main]
    types: [completed]

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script_stop: true
          script: |
            set -euo pipefail
            cd /opt/tg-bot
            git fetch --all --tags --prune
            git reset --hard origin/main
            docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
            docker image prune -f
            echo "=== deployed $(git rev-parse --short HEAD) ==="
```

- [ ] **Step 8.2: YAML syntax-check**

```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))"
```
Expected: ноль вывода.

- [ ] **Step 8.3: Commit**

```bash
git add .github/workflows/deploy.yml
git commit -m "ci: add deploy workflow — SSH + git reset + compose up --build"
```

---

## Task 9: Operational runbook

**Files:**
- Create: `docs/deployment/README.md`

**Rationale:** Будущий-ты (или сменщик) должен по этому документу сделать первый setup, откатить релиз, сменить токен, залогиниться в psql.

- [ ] **Step 9.1: Создать директорию**

```bash
mkdir -p docs/deployment
```

- [ ] **Step 9.2: Создать `docs/deployment/README.md`**

```markdown
# Deployment Runbook

VPS: Hetzner CAX11, Ubuntu 24.04 (ARM64), IP зафиксирован в GitHub Secrets (`SSH_HOST`).

## First-time setup

1. **Create VM in Hetzner Cloud**: CAX11, Ubuntu 24.04, NBG1, привязать свой SSH-ключ.
2. **Verify SSH as root:**
   ```bash
   ssh root@<IP>
   ```
3. **Run bootstrap** (скачивает себя из main):
   ```bash
   curl -fsSL https://raw.githubusercontent.com/VanikVardanyan/grancvi/main/scripts/bootstrap-vps.sh | bash
   ```
   Скрипт идемпотентный: повторный запуск безопасен.
4. **Create `.env`:**
   ```bash
   sudo -u deploy cp /opt/tg-bot/.env.prod.example /opt/tg-bot/.env
   sudo -u deploy nano /opt/tg-bot/.env
   ```
   Заполнить: `BOT_TOKEN`, `ADMIN_TG_IDS`, `SENTRY_DSN`, `POSTGRES_PASSWORD` (`openssl rand -base64 24`), и синхронно обновить `DATABASE_URL`.
5. **First build & start** (под `deploy`):
   ```bash
   sudo -u deploy bash -c 'cd /opt/tg-bot && docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build'
   ```
6. **Verify health:**
   ```bash
   sudo -u deploy docker compose -f /opt/tg-bot/docker-compose.yml -f /opt/tg-bot/docker-compose.prod.yml ps
   sudo -u deploy docker inspect tg-bot-app-1 --format='{{.State.Health.Status}}'
   ```
   Через 60–90 секунд статус должен быть `healthy`.

## Deploy key for CI

1. **On your Mac:**
   ```bash
   ssh-keygen -t ed25519 -f ~/.ssh/grancvi-deploy -N '' -C 'grancvi-deploy'
   ```
2. **Append pub key on VPS** (as root or через любого пользователя с доступом):
   ```bash
   cat ~/.ssh/grancvi-deploy.pub | ssh root@<IP> 'cat >> /home/deploy/.ssh/authorized_keys'
   ```
3. **Verify login as deploy** (должно пустить без пароля):
   ```bash
   ssh -i ~/.ssh/grancvi-deploy deploy@<IP> 'echo ok'
   ```
4. **GitHub repo Settings → Secrets and variables → Actions → New repository secret**:
   - `SSH_HOST` = `<VPS IPv4>`
   - `SSH_USER` = `deploy`
   - `SSH_PRIVATE_KEY` = вывод `cat ~/.ssh/grancvi-deploy` (включая `-----BEGIN OPENSSH PRIVATE KEY-----` и `-----END OPENSSH PRIVATE KEY-----`)

## Secrets reference

### GitHub Actions secrets (repo Settings → Secrets)

| Name | Value |
|---|---|
| `SSH_HOST` | VPS IPv4 |
| `SSH_USER` | `deploy` |
| `SSH_PRIVATE_KEY` | приватный ключ deploy-only, без пароля |

### `/opt/tg-bot/.env` on VPS

| Key | Notes |
|---|---|
| `BOT_TOKEN` | @BotFather |
| `ADMIN_TG_IDS` | Telegram user id мастера (через запятую) |
| `SENTRY_DSN` | sentry.io → Project → Settings → Client Keys |
| `LOG_LEVEL` | `INFO` |
| `POSTGRES_USER` | `botik` |
| `POSTGRES_PASSWORD` | `openssl rand -base64 24` |
| `POSTGRES_DB` | `botik` |
| `DATABASE_URL` | `postgresql+asyncpg://botik:${POSTGRES_PASSWORD}@postgres:5432/botik` |
| `REDIS_URL` | `redis://redis:6379/0` |
| `DEFAULT_TIMEZONE` | `Asia/Yerevan` |

## Day-to-day operations

### Change a secret (e.g. rotate bot token)

```bash
ssh deploy@<IP>
cd /opt/tg-bot
nano .env     # edit
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d   # recreate app only
```

### Rollback last release

**Preferred (via git):**
```bash
# on your Mac
git revert <sha> && git push origin main
# CI auto-deploys the revert in ~2 min
```

**Emergency (directly on VPS):**
```bash
ssh deploy@<IP>
cd /opt/tg-bot
git reset --hard <old-sha>
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
```
(Риск: если потом запушишь новый коммит, CI накатит свежий `origin/main` поверх ручного отката.)

### Access psql

```bash
ssh deploy@<IP>
cd /opt/tg-bot
docker compose -f docker-compose.yml -f docker-compose.prod.yml exec postgres psql -U botik botik
```

### Tail logs

```bash
ssh deploy@<IP>
cd /opt/tg-bot
docker compose -f docker-compose.yml -f docker-compose.prod.yml logs -f --tail 300 app
```

### Restart only app (preserving db/redis state)

```bash
ssh deploy@<IP>
cd /opt/tg-bot
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build app
```

### Check healthcheck

```bash
ssh deploy@<IP>
docker inspect tg-bot-app-1 --format='{{.State.Health.Status}}'
docker inspect tg-bot-app-1 --format='{{json .State.Health}}' | jq
```

## Troubleshooting

**App unhealthy forever:**
- `docker compose logs --tail 300 app` — есть ли Python traceback?
- `docker compose exec app python /app/scripts/healthcheck.py` — что падает? (Обычно — токен неверный или нет интернета.)

**Миграции падают при старте:**
- `docker compose logs app | head -50` — ищи `alembic` ошибки.
- Rollback коммита с миграцией, push.

**Deploy workflow красный:**
- GitHub Actions → Deploy → логи SSH. Обычно: истёк `SSH_PRIVATE_KEY`, сменился IP VPS, или `authorized_keys` на VPS почищен.
- Переключиться на ручной деплой (п. «Rollback» → «Emergency») и расследовать secrets.
```

- [ ] **Step 9.3: Commit**

```bash
git add docs/deployment/README.md
git commit -m "docs(deploy): operational runbook for first-setup, rollback, secrets"
```

---

## Task 10: Final local gates + sanity check

**Files:** none (validation only)

**Rationale:** Перед push в origin — убедиться, что CI не упадёт с первого раза из-за локально пропущенного формата / mypy.

- [ ] **Step 10.1: Полные гейты**

```bash
source .venv/bin/activate
ruff check .
ruff format --check .
mypy src/
pytest -q
```
Expected: всё зелёное. Особо: `pytest` включает новые `test_main_sentry.py` (3) и `test_healthcheck.py` (7).

- [ ] **Step 10.2: Проверить, что все файлы на месте**

```bash
ls .github/workflows/ docker-compose.prod.yml .env.prod.example scripts/healthcheck.py scripts/bootstrap-vps.sh docs/deployment/README.md
```
Expected: каждый путь существует.

- [ ] **Step 10.3: Summary diff против origin/main**

```bash
git log --oneline origin/main..HEAD | wc -l
git diff --stat origin/main..HEAD | tail -5
```
Expected: commits увеличились на ~9 относительно pre-flight, stat показывает изменённые файлы.

- [ ] **Step 10.4: Local tree clean**

```bash
git status
```
Expected: `working tree clean`.

---

## Task 11: 🧑 USER ACTION — Push local main to origin

**Rationale:** 41+ коммит лежит локально. Их нужно запушить на GitHub, чтобы CI увидел код (включая новые workflows). На этом пуше CI запустится впервые; deploy workflow тоже запустится, но упадёт без SSH secrets — это ожидаемо, исправим в следующих tasks.

- [ ] **Step 11.1: Backup-тег перед первым прод-push (на случай если что-то пойдёт не так)**

```bash
git tag pre-epic-8-2-push
```

- [ ] **Step 11.2: Push main + tags**

```bash
git push origin main --follow-tags
```
Expected: `Writing objects: ... X% (N/N), done.`, GitHub примет все коммиты и теги.

- [ ] **Step 11.3: Проверить в браузере**

Открыть https://github.com/VanikVardanyan/grancvi/actions. Должна идти первая запуск workflow `ci`. Через 2–3 минуты ci завершится зелёным.

Expected:
- `ci` → **success** ✅
- `deploy` → **failure** ❌ (SSH_HOST/USER/KEY не заданы — ожидаемо).

Если `ci` красный — остановись и разбери лог. Чаще всего проблема: `mypy` в CI строже (новая версия) либо `pytest` флакнул из-за timing. Fix → push → повтор.

---

## Task 12: 🧑 USER ACTION — Bootstrap VPS

**Prereq:** Task 11 — репа обновлена на GitHub.

- [ ] **Step 12.1: SSH в VPS под root**

```bash
ssh root@94.130.149.91
```

- [ ] **Step 12.2: Запустить bootstrap**

```bash
curl -fsSL https://raw.githubusercontent.com/VanikVardanyan/grancvi/main/scripts/bootstrap-vps.sh | bash
```
Expected: последовательно видишь `==> Step 1/6` ... `==> Step 6/6`, в конце баннер с инструкциями.

- [ ] **Step 12.3: Проверить, что docker работает**

```bash
docker --version
docker compose version
```
Expected: Docker Engine ≥ 27.x, Compose plugin ≥ v2.x.

- [ ] **Step 12.4: Проверить, что юзер `deploy` создан и в группе `docker`**

```bash
id deploy
```
Expected: `... groups=... docker(NNN) ...`.

- [ ] **Step 12.5: Проверить, что UFW активен и впускает только 22**

```bash
ufw status
```
Expected:
```
Status: active
22/tcp    ALLOW    Anywhere
22/tcp (v6) ALLOW  Anywhere (v6)
```

- [ ] **Step 12.6: Проверить, что репа склонирована**

```bash
sudo -u deploy ls /opt/tg-bot
```
Expected: видны `src`, `migrations`, `Dockerfile`, `docker-compose.prod.yml`, `scripts`, etc.

---

## Task 13: 🧑 USER ACTION — Sentry signup + DSN

**Prereq:** доступ к браузеру.

- [ ] **Step 13.1: Регистрация на sentry.io**

Открыть https://sentry.io/signup/, зарегистрироваться (free tier — 5k событий/мес).

- [ ] **Step 13.2: Создать проект**

Create Project → Platform: **Python** → Project name: `grancvi-tg-bot`.

- [ ] **Step 13.3: Скопировать DSN**

После создания Sentry покажет DSN вида `https://<publickey>@o<org>.ingest.sentry.io/<projectid>`. Скопировать целиком.

---

## Task 14: 🧑 USER ACTION — Create .env on VPS

**Prereq:** Task 13 — DSN в руках.

- [ ] **Step 14.1: SSH root → создать .env из шаблона под deploy**

```bash
ssh root@94.130.149.91
sudo -u deploy cp /opt/tg-bot/.env.prod.example /opt/tg-bot/.env
```

- [ ] **Step 14.2: Сгенерировать POSTGRES_PASSWORD**

```bash
openssl rand -base64 24
```
Скопировать вывод (типа `xY3kPq9mNvLwR7tHsE5zQ8bDfJ6aCg==`).

- [ ] **Step 14.3: Отредактировать .env**

```bash
sudo -u deploy nano /opt/tg-bot/.env
```

Заполнить:
- `BOT_TOKEN=` — токен из @BotFather
- `ADMIN_TG_IDS=` — твой Telegram user id (узнать: @userinfobot)
- `SENTRY_DSN=` — из Task 13
- `POSTGRES_PASSWORD=` — сгенерированный в Step 14.2
- `DATABASE_URL=` — `postgresql+asyncpg://botik:<SAME_PASSWORD>@postgres:5432/botik`
- Остальное — как есть в шаблоне.

- [ ] **Step 14.4: Проверить права файла**

```bash
sudo -u deploy ls -la /opt/tg-bot/.env
```
Expected: владелец `deploy`, права `-rw-r--r--` или `-rw-------`.

- [ ] **Step 14.5: Проверить, что .env НЕ в git**

```bash
cd /opt/tg-bot
sudo -u deploy git status
```
Expected: `nothing to commit, working tree clean` — `.env` должен быть в `.gitignore`.

---

## Task 15: 🧑 USER ACTION — First manual build on VPS

**Prereq:** Tasks 12–14 выполнены.

- [ ] **Step 15.1: Первый build & start под deploy**

```bash
sudo -u deploy bash -c 'cd /opt/tg-bot && docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build'
```
Expected: билд ARM-образа (~3-5 минут первый раз), затем создаются контейнеры `tg-bot-postgres-1`, `tg-bot-redis-1`, `tg-bot-app-1`.

- [ ] **Step 15.2: Проверить контейнеры**

```bash
sudo -u deploy docker compose -f /opt/tg-bot/docker-compose.yml -f /opt/tg-bot/docker-compose.prod.yml ps
```
Expected: все три `Up`, postgres и redis `Up (healthy)`.

- [ ] **Step 15.3: Подождать 90 секунд, проверить health app**

```bash
sleep 90 && docker inspect tg-bot-app-1 --format='{{.State.Health.Status}}'
```
Expected: `healthy`.

- [ ] **Step 15.4: Посмотреть логи app**

```bash
sudo -u deploy docker compose -f /opt/tg-bot/docker-compose.yml -f /opt/tg-bot/docker-compose.prod.yml logs --tail 50 app
```
Expected:
- `Running upgrade ... → ...` (Alembic мигрирует).
- `{"event": "bot_starting", ...}`.
- `Scheduler started`.
- `Start polling`.
- `Run polling for bot @<bot_username>`.

- [ ] **Step 15.5: Smoke-тест бота из Telegram**

Открыть бота в Telegram, отправить `/start`. Должен ответить. Если нет — проверить токен в `.env`.

---

## Task 16: 🧑 USER ACTION — Deploy key + GitHub Secrets

**Prereq:** Task 15 — бот работает руками.

- [ ] **Step 16.1: На Маке — сгенерировать deploy-only ключ**

```bash
ssh-keygen -t ed25519 -f ~/.ssh/grancvi-deploy -N '' -C 'grancvi-deploy'
```
Expected: созданы `~/.ssh/grancvi-deploy` и `~/.ssh/grancvi-deploy.pub`.

- [ ] **Step 16.2: Добавить pub-ключ в authorized_keys на VPS**

```bash
cat ~/.ssh/grancvi-deploy.pub | ssh root@94.130.149.91 'cat >> /home/deploy/.ssh/authorized_keys'
```

- [ ] **Step 16.3: Проверить SSH под deploy**

```bash
ssh -i ~/.ssh/grancvi-deploy deploy@94.130.149.91 'whoami && docker --version'
```
Expected: `deploy` + версия Docker. Без запроса пароля.

- [ ] **Step 16.4: Добавить GitHub Secrets**

Открыть https://github.com/VanikVardanyan/grancvi/settings/secrets/actions → **New repository secret** (три раза):

| Name | Value |
|---|---|
| `SSH_HOST` | `94.130.149.91` |
| `SSH_USER` | `deploy` |
| `SSH_PRIVATE_KEY` | Вывод `cat ~/.ssh/grancvi-deploy` — **включая** строки `-----BEGIN OPENSSH PRIVATE KEY-----` и `-----END OPENSSH PRIVATE KEY-----`, **без** хвостового пробела. |

Дважды нажать **Add secret** для каждого.

- [ ] **Step 16.5: Проверить, что Secrets видны в списке**

На той же странице должны быть все три секрета (значения скрыты, но имена видны).

---

## Task 17: 🧑 USER ACTION — Trigger auto-deploy and verify

**Prereq:** Tasks 11, 15, 16 — код на GitHub, бот работает вручную, Secrets заданы.

- [ ] **Step 17.1: Сделать триггер-коммит**

На Маке, в чистом дереве:

```bash
git commit --allow-empty -m "chore: trigger first auto-deploy"
git push origin main
```

- [ ] **Step 17.2: Следить за CI**

Открыть https://github.com/VanikVardanyan/grancvi/actions. Через 10–20 секунд появится новый run `ci`. Дождаться ✅.

- [ ] **Step 17.3: Следить за deploy**

После зелёного ci появится run `deploy`. Открыть его → Deploy over SSH → логи должны показать:
- `git fetch --all --tags --prune`
- `HEAD is now at <short-sha> chore: trigger first auto-deploy`
- Docker-билд + up -d
- `=== deployed <short-sha> ===`

Expected: workflow зелёный.

- [ ] **Step 17.4: Подтвердить HEAD на VPS совпадает с origin/main**

```bash
ssh deploy@94.130.149.91 'cd /opt/tg-bot && git rev-parse HEAD'
git rev-parse origin/main    # на Маке
```
Expected: одинаковые SHA.

---

## Task 18: Smoke test — 10 acceptance criteria

- [ ] **Step 18.1: AC 1 — CI работает**

Открыть последний `ci` run в Actions → все 4 гейта зелёные (Ruff lint / Ruff format check / Mypy / Pytest).

- [ ] **Step 18.2: AC 2 — Автодеплой работает**

На Маке:
```bash
git log --oneline origin/main | head -1
ssh deploy@94.130.149.91 'cd /opt/tg-bot && git log --oneline | head -1'
```
Expected: одинаковые SHA.

- [ ] **Step 18.3: AC 3 — Security boundary (только 22)**

С Мака:
```bash
nmap -p 1-10000 94.130.149.91 2>&1 | grep open
```
Expected: только `22/tcp open ssh` (или nmap-эквивалент). Если виден 5432/6379 — UFW не работает / compose.prod.yml не применён.

- [ ] **Step 18.4: AC 4 — SSH под паролем заблокирован**

```bash
ssh -o PreferredAuthentications=password -o PubkeyAuthentication=no root@94.130.149.91 2>&1 | head -3
```
Expected: `Permission denied (publickey)` или подобное.

- [ ] **Step 18.5: AC 5 — Root SSH без ключа заблокирован**

```bash
grep -E '^PermitRootLogin' /etc/ssh/sshd_config   # на VPS
```
Выполнить на VPS (под root через ключ):
```bash
ssh root@94.130.149.91 'grep -E "^PermitRootLogin" /etc/ssh/sshd_config'
```
Expected: `PermitRootLogin prohibit-password`.

- [ ] **Step 18.6: AC 6 — Healthcheck работает**

```bash
ssh deploy@94.130.149.91 'docker inspect tg-bot-app-1 --format="{{.State.Health.Status}}"'
```
Expected: `healthy`.

- [ ] **Step 18.7: AC 7 — Sentry алертит**

На Маке создать временный тест-хэндлер, провоцирующий исключение:
```python
# src/handlers/debug.py (временно)
from aiogram import Router
from aiogram.filters import Command
router = Router()

@router.message(Command("sentrytest"))
async def sentry_test(message) -> None:
    raise RuntimeError("Sentry smoke test")
```

Подключить роутер в `build_root_router()`, запушить, дождаться деплоя.

В Telegram: `/sentrytest` → sentry.io проверить, что событие появилось в проекте `grancvi-tg-bot`. Email должен прийти на адрес при регистрации.

**После подтверждения** — откатить тестовый хэндлер: `git revert <sha> && git push` (CI сам удалит).

- [ ] **Step 18.8: AC 8 — Миграции применяются при старте**

```bash
ssh deploy@94.130.149.91 'cd /opt/tg-bot && docker compose -f docker-compose.yml -f docker-compose.prod.yml logs app | grep -i alembic'
```
Expected: видны строки `Running upgrade ...` из Alembic (после каждого рестарта контейнера).

- [ ] **Step 18.9: AC 9 — Runbook проверен**

Визуально открыть `docs/deployment/README.md`, пробежаться по 4 ключевым сценариям (first setup / change secret / rollback / psql). Убедиться, что путь файла, SSH-хост, названия сервисов совпадают с реальностью.

- [ ] **Step 18.10: AC 10 — Rollback через git revert**

На Маке:
```bash
git log --oneline | head -3   # запомни текущий HEAD = X
# создай неопасный коммит
echo "# deploy smoketest" >> README.md
git add README.md && git commit -m "chore: smoketest comment"
git push origin main
# дождись зелёного ci + deploy (~2-3 мин)
ssh deploy@94.130.149.91 'cat /opt/tg-bot/README.md | tail -1'
# → "# deploy smoketest"

# Теперь revert
git revert --no-edit HEAD
git push origin main
# дождись зелёного deploy
ssh deploy@94.130.149.91 'cat /opt/tg-bot/README.md | tail -1'
# → НЕ должно быть "# deploy smoketest"
```
Expected: revert прокатывает откат изменений без потери данных БД.

---

## Task 19: Tag the epic

- [ ] **Step 19.1: Локальный тег на последнем коммите**

```bash
git tag v0.8.2-epic-8-2-deploy
```

- [ ] **Step 19.2: Push тега**

```bash
git push origin v0.8.2-epic-8-2-deploy
```

- [ ] **Step 19.3: Чистка backup-тега**

```bash
git tag -d pre-epic-8-2-push
```
(Origin не получал этот тег — `--follow-tags` пушит только annotated/signed, а мы делали lightweight. Проверь `git ls-remote origin refs/tags/pre-epic-8-2-push` — пусто.)

- [ ] **Step 19.4: Update memory file**

Открыть `/Users/vanik/.claude/projects/-Users-vanik-Desktop-projects-working-projects-tg-bot/memory/project_current_epic.md` и:
- Переместить Epic 7 в closed, добавить Epic 8.2 как текущий-закрытый.
- Зафиксировать VPS IP, acceptance pass, тег.
- Указать, что следующий эпик — 8.3 (backups).

---

## Risks & recoveries (during execution)

**First `ci` run красный на push (Task 11):**
- Чаще всего — mypy strict / ruff чуть новее локального. Смотри лог, исправляй, коммить, пушь.
- Если тесты флакнули — `Re-run failed jobs` на GitHub.

**Bootstrap падает на step 2 (Docker install):**
- `apt-get` network issue — через минуту попробуй заново, скрипт идемпотентен.

**Первый `docker compose up -d --build` на VPS падает:**
- ARM-wheel для какого-то пакета отсутствует → `apt install build-essential` вручную и ретрай. (Все наши deps должны иметь aarch64 wheels — если нет, сообщи, обсудим.)
- Postgres не стартует (`permission denied on volume`) → удалить volume `docker volume rm tg-bot_pg_data` и попробовать заново (данных ещё нет).

**Deploy workflow падает с `Permission denied (publickey)`:**
- `SSH_PRIVATE_KEY` скопирован без `-----BEGIN---`/`-----END---` строк. Пере-добавить Secret.
- `authorized_keys` на VPS почищен. `scp` ключ заново.

**Healthcheck статус `unhealthy` через 90 секунд:**
- `docker compose exec app python /app/scripts/healthcheck.py` — ручной запуск покажет реальную ошибку.
- Обычно `BOT_TOKEN` неверный или DNS не резолвится.

---

## What this plan does NOT cover

- **pg_dump → Backblaze B2 backups** — Epic 8.3.
- **External uptime monitoring** (UptimeRobot и т.п.) — отдельной задачей когда появятся пользователи.
- **Fail2ban** — UFW + no-password-ssh считаются достаточными для starter-setup.
- **Unattended security upgrades** — документируется в Tasks 9 runbook как ручная процедура, автоматизация авто-reboot'а отложена.
- **GHCR image push** — в этой итерации собираем на VPS; если деплой начнёт болеть > 5 мин, мигрируем отдельно.
- **Second master на этом же VPS** — одна машина на одного мастера; multi-tenant — другой эпик.

---

## Completion criteria

План считается выполненным когда:
1. Все 19 tasks имеют зелёные чекбоксы.
2. `git rev-parse v0.8.2-epic-8-2-deploy` на Маке совпадает с `origin/refs/tags/v0.8.2-epic-8-2-deploy`.
3. Бот отвечает в Telegram.
4. `https://github.com/VanikVardanyan/grancvi/actions` показывает зелёный history на последних 2+ прогонах.
5. `memory/project_current_epic.md` обновлён.
